# Stop Loss and Trailing Stop Implementation

## Overview

The BacktestFramework now supports protective stop orders to limit losses on portfolio positions.

This feature implements both **fixed stop loss** and **trailing stop** orders based on backtrader's native order execution types.
